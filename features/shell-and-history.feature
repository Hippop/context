# language: zh-CN
功能: 压缩 Shell 和历史噪声并保留失败证据

  场景: 测试通过项折叠而失败完整保留
    假如 500 个 PASSED 和 1 个 FAILED
    当执行测试输出压缩
    那么 PASSED 应折叠为计数
    并且 FAILED、断言差异和最终摘要应完整存在

  场景: Drain 风格日志模板保持顺序
    假如连续日志只有时间、请求 ID 不同
    当执行模板压缩
    那么常量模板只输出一次
    并且变量表能按原顺序表达每条事件

  场景: 最近历史窗口受保护
    假如历史中有重复工具结果
    当消息 transform 执行
    那么最近 N 个工具结果保持原样
    并且更早的精确重复结果替换为引用

  场景: 重复错误形成摘要
    假如同一错误在多次重试中出现
    当历史压缩或 compaction 执行
    那么旧错误栈应聚合
    并且保留错误签名、最新状态和项目栈帧
