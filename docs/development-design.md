开发方案：

一、前期需求理解
1、grill-me-doc
需求澄清

2、压缩算法探索
搜索相关开源项目，下载源码，分析总结优缺点

3、DDD 统一术语

4、明确各场景压缩算法

5、非功能性要求
Token不能增加
失败不影响正常流程
记录输入输出的文件类型压缩前后Token和压缩比和时间消耗
记录不同任务总体Token压缩比和时间消耗

二、测试评估
BDD与TDD双循环
1、BDD 行为驱动开发 输出行为测试场景
2、TDD 测试驱动开发 输出测试用例
3、Benchmarks 测试基准 
3.1 文本测试：不同文本长度的压缩前后压缩比和时间消耗
有测试用例文档：包含具体的输入文件或命令行输出，生成测试报告，包含压缩比和时间消耗
放在benchmark目录中

3.2 集成测试（End-to-End A/B测试）：不同任务总体Token压缩比和时间消耗
有测试场景文档：包含具体场景，使用真实的opencode进行AB测试，生成测试报告，包含压缩比和时间消耗
根据研发过程中涉及到的场景，构建不同的Benchmarks
设计生成一个代码库，maven项目，涉及JSON文件、XML文件、测试文件、代码文件
基于这个代码库，设计7个任务给Opencode AB测试执行：代码生成文档、根据需求生成设计文档、代码生成、测试生成、测试运行和修改、代码评审，编译运行

三、架构设计
语义建模架构设计（SMDD）
输出adr文档

四、代码开发结合测试
